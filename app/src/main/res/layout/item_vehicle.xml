<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:descendantFocusability="blocksDescendants"
    android:focusable="true"
    android:padding="@dimen/default_indentation_size">

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/actvVehicleId"
        android:layout_width="0dp"
        android:layout_height="@dimen/item_line_height"
        android:layout_marginEnd="@dimen/default_indentation_size"
        android:gravity="center_vertical|start"
        android:hint="@string/hint_vehicle_id"
        android:maxLines="1"
        android:textStyle="italic"
        app:layout_constraintEnd_toStartOf="@+id/actvVehicleStatus"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/actvVehicleStatus"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/item_line_height"
        android:gravity="center_vertical|end"
        android:hint="@string/hint_default_value"
        android:textColor="@color/black"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>